name: React - Laboratorio Final
on:
  push:
    branches:
      - feat/lab_final_devsecops

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:

      - name: Install Dependencies
        run: npm install  

      - name: Mensaje avance
        run: echo "Instalación de dependencias"

